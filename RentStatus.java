/**
 * Enumerácia predstavujúca možné stavy prenájmu.
 * Obsahuje stavy "RENTED" (prenajaté) a "RETURNED" (vrátené).
 * 
 * @author (Your Name)
 * @version (Version number or date)
 */
public enum RentStatus {
    /** Vozidlo je prenajaté. */
    RENTED,
    /** Vozidlo je vrátené. */
    RETURNED
}
